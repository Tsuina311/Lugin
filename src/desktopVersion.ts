/**
 * Desktop overlay code version — what you look at to know a reload picked up
 * the latest change.
 *
 * Not tied to git. Base is `0.1`; the third number steps on every chat turn that
 * changes desktop (extension) code: `0.1.0`, `0.1.1`, … `0.1.13`, …
 * Bump it in the same edit as the fix.
 */
export const DESKTOP_VERSION = '0.1.65';
