# Files EAS Build does not need from the monorepo root.
# Keep fingerprint/build focused on the mobile workspace + shared source.
# Do NOT ignore yarn.lock, package.json, or .yarnrc.yml — EAS needs Berry.

.scan-fixtures/
.scan-real/
.scan-corpus/
.lugin-prices/
dist/
dist-web/
dist-smoke/
release/
.extension-key.pem
workers/
.git/
.yarn/cache/
# Keep .yarn/releases (committed Berry binary) — required for EAS Build.
**/node_modules/
**/.expo/
mobile/android/
mobile/ios/
*.md
!docs/MOBILE-DEPLOYMENT.md
!docs/MOBILE-NATIVE.md
